'use strict'
//Задание 1

// let elem = document.querySelector('#elem');
// let currentSize = parseFloat(window.getComputedStyle(elem).getPropertyValue('font-size'));
// let newSize = currentSize + 2;
// elem.style.fontSize = newSize + 'px';

//Задание 2
// let elem = document.querySelector('#elem');
// let currentSize = parseInt(window.getComputedStyle(elem).getPropertyValue('font-size'));
// let newSize = currentSize + 2;
// elem.style.fontSize = newSize + 'px';

//Задание 3

// let elem = document.querySelector('div');
// let currentSize = parseFloat(window.getComputedStyle(elem).getPropertyValue('font-size'));
// let newSize = currentSize + 2;
// elem.style.fontSize = newSize + 'em';

// Задание 4 
// const elem = document.getElementById('elem');
// const increaseSizeButton = document.getElementById('increaseSizeButton');
// increaseSizeButton.addEventListener('click', function() {
//   const currentWidth = parseFloat(window.getComputedStyle(elem).getPropertyValue('width'));
//   const currentHeight = parseFloat(window.getComputedStyle(elem).getPropertyValue('height'));
//   elem.style.width = (currentWidth + 50) + 'px';
//   elem.style.height = (currentHeight + 50) + 'px';
// });

// Задание 5
// const elem = document.getElementById('elem');
// const increaseSizeButton = document.getElementById('increaseSizeButton');
// increaseSizeButton.addEventListener('click', function() {
//   const currentWidth = parseFloat(window.getComputedStyle(elem).getPropertyValue('width'));
//   const currentHeight = parseFloat(window.getComputedStyle(elem).getPropertyValue('height'));
//   const newWidth = currentWidth * 1.1; 
//   const newHeight = currentHeight * 1.1; 
//   elem.style.width = newWidth + 'px';
//   elem.style.height = newHeight + 'px';
// });